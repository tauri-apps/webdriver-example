# Tauri WebDriver tests with [Selenium]

This example uses the WebDriver client [Selenium] (the Node.js version) and a custom test suite written with `mocha` and
`chai`.

[Selenium]: https://selenium.dev/
